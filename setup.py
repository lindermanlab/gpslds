from setuptools import setup, find_packages

setup(
    name='gpslds',
    version='0.1',
    description='Gaussian process switching linear dynamical systems',
    packages=find_packages(),
    python_requires='>=3.9',
    )
